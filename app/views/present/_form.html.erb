<%= form_with model: @present, local: true do |form| %>

  <% if @present.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@present.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @present.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>
